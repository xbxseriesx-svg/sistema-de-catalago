--- a/src/editor/Canvas.tsx
+++ b/src/editor/Canvas.tsx
@@ -3,6 +3,7 @@
 import { NodeRenderer } from './NodeRenderer';
 import type { GuideLine } from './smartGuides';
 import { getResponsiveRect, responsivePatch } from './utils';
+import { PageResizeHandle } from '../cloud/PageResizeHandle';
 
 interface CanvasProps {
   guides: GuideLine[];
@@ -40,7 +41,7 @@
     return bottom;
   })();
 
-  const effectivePageHeight = Math.max(pageRect.height, contentBottom + 480, viewportHeight * 2);
+  const effectivePageHeight = Math.max(pageRect.height, contentBottom + 120, 480);
 
   const screenToCanvas = useCallback(
     (screenX: number, screenY: number) => {
@@ -291,6 +292,10 @@
           <NodeRenderer node={displayPage} parentId={null} isAncestorSelected={false} guides={guides} setGuides={setGuides} />
         )}
 
+        {page && !state.previewMode && (
+          <PageResizeHandle page={page} pageRect={pageRect} contentBottom={contentBottom} effectivePageHeight={effectivePageHeight} />
+        )}
+
         {!state.previewMode && displayPage && Array.from({ length: Math.max(0, Math.ceil(effectivePageHeight / viewportHeight) - 1) }).map((_, index) => {
           const y = (index + 1) * viewportHeight;
           return (
--- a/src/editor/PropertiesPanel.tsx
+++ b/src/editor/PropertiesPanel.tsx
@@ -16,6 +16,7 @@
   AlignVerticalJustifyEnd,
   ChevronRight,
 } from 'lucide-react';
+import { ButtonActionEditor } from '../cloud/ButtonActionEditor';
 
 export function PropertiesPanel() {
   const { state, dispatch, getNode, getBreadcrumb: getBc } = useEditor();
@@ -377,6 +378,7 @@
       <Row label="Tamanho">
         <Slider value={(s.fontSize as number) || 14} min={8} max={32} onChange={(v) => updateStyle('fontSize', v)} suffix="px" />
       </Row>
+      <ButtonActionEditor node={node} />
       {mode === 'professional' && (
         <>
           <Row label="Contorno">
--- a/src/editor/CatalogPanel.tsx
+++ b/src/editor/CatalogPanel.tsx
@@ -3,6 +3,7 @@
 import { useCatalog } from '../catalog/store';
 import { createProductGroup, createShowcaseGroup } from '../catalog/editorNodes';
 import { useEditor } from './store';
+import { CatalogImportPanel } from '../cloud/CatalogImportPanel';
 
 export function CatalogPanel() {
   const catalog = useCatalog();
@@ -44,6 +45,7 @@
           </div>
           <span className="rounded bg-blue-500/10 px-1.5 py-0.5 text-[10px] font-semibold text-blue-400">{catalog.products.length}</span>
         </div>
+        <CatalogImportPanel />
         <div className="relative">
           <Search size={13} className="absolute left-2 top-1/2 -translate-y-1/2 text-zinc-500" />
           <input
--- a/src/editor/nodeContent.tsx
+++ b/src/editor/nodeContent.tsx
@@ -190,6 +190,11 @@
       </div>
     );
   }
+  if (type === 'button' || type === 'productbutton') {
+    const text = (props.text as string) || '';
+    if (isEditing) return text;
+    return <span style={{ display: 'flex', width: '100%', height: '100%', boxSizing: 'border-box', alignItems: (node.styles.alignItems as React.CSSProperties['alignItems']) || 'center', justifyContent: (node.styles.justifyContent as React.CSSProperties['justifyContent']) || 'center', textAlign: (node.styles.textAlign as React.CSSProperties['textAlign']) || 'center', padding: '0 10px', whiteSpace: 'pre-wrap', wordBreak: 'break-word' }}>{text}</span>;
+  }
   if (isTextTypeRenderer(type)) {
     const text = (props.text as string) || '';
     if (isEditing) {
--- a/src/editor/NodeRenderer.tsx
+++ b/src/editor/NodeRenderer.tsx
@@ -6,6 +6,7 @@
 import { isContainerType, isTextType, isImageType, ELEMENT_REGISTRY } from './registry';
 import { calculateSnap, getAbsoluteRect, getAllRects, type GuideLine } from './smartGuides';
 import { getResponsiveRect, responsivePatch, snapTo } from './utils';
+import { executeNodeAction } from '../cloud/buttonActions';
 
 interface NodeRendererProps {
   node: EditorNode;
@@ -375,6 +376,7 @@
   const showOutline = isSelected || (hovered && !isAncestorSelected);
   const outlineColor = isSelected ? '#3b82f6' : 'rgba(59, 130, 246, 0.4)';
   const isPage = node.type === 'page';
+  const isActionButton = (node.type === 'button' || node.type === 'productbutton') && String(node.props?.actionType || 'none') !== 'none';
 
   const containerStyle: React.CSSProperties = {
     position: 'absolute',
@@ -393,6 +395,7 @@
       style={containerStyle}
       data-node-id={node.id}
       data-node-type={node.type}
+      data-node-name={node.name || ''}
       onPointerDown={onPointerDown}
       onPointerMove={onPointerMove}
       onPointerUp={onPointerUp}
@@ -402,12 +405,17 @@
       onMouseLeave={() => dispatch({ type: 'SET_HOVER', id: null })}
     >
       <div
+        role={state.previewMode && isActionButton ? 'button' : undefined}
+        tabIndex={state.previewMode && isActionButton ? 0 : undefined}
+        onClick={state.previewMode && isActionButton ? (event) => { event.stopPropagation(); executeNodeAction(node); } : undefined}
+        onKeyDown={state.previewMode && isActionButton ? (event) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); executeNodeAction(node); } } : undefined}
         style={{
           ...styleObj(node),
           position: 'relative',
           width: '100%',
           height: '100%',
           pointerEvents: 'auto',
+          cursor: state.previewMode && isActionButton ? 'pointer' : undefined,
           overflow: 'hidden',
         }}
       >