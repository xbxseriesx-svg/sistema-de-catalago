import type { EditorNode } from '../editor/types';
import { useEditor } from '../editor/store';
import { useCatalog } from '../catalog/store';
import { resolveNodeAction } from './buttonActions';

const inputClass='w-full rounded border border-zinc-700 bg-zinc-900 px-2 py-1.5 text-[10px] text-zinc-200 outline-none focus:border-blue-500';
const SECTION_OPTIONS=[['#catalogo','Catálogo / produtos'],['#promocoes','Promoções'],['#quem-somos','Quem somos'],['#como-comprar','Como comprar'],['#depoimentos','Depoimentos'],['#contato','Contato / representante']];
export function ButtonActionEditor({node}:{node:EditorNode}){const {dispatch}=useEditor();const catalog=useCatalog();const prop=(key:string,value:unknown)=>dispatch({type:'UPDATE_NODE_PROP',id:node.id,key,value});const style=(key:string,value:unknown)=>dispatch({type:'UPDATE_NODE_STYLE',id:node.id,key,value});const type=String(node.props?.actionType||'none');const automatic=resolveNodeAction(node);return <div className="mt-3 space-y-2 rounded-lg border border-blue-500/20 bg-blue-500/5 p-2.5"><div className="text-[10px] font-semibold uppercase tracking-wide text-blue-300">Alinhamento e função</div><label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Alinhamento do texto</span><select value={String(node.styles.justifyContent||'center')} onChange={e=>{style('display','flex');style('alignItems','center');style('justifyContent',e.target.value);style('textAlign',e.target.value==='flex-start'?'left':e.target.value==='flex-end'?'right':'center')}} className={inputClass}><option value="flex-start">Esquerda</option><option value="center">Centro</option><option value="flex-end">Direita</option></select></label><label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Função ao clicar</span><select value={type} onChange={e=>prop('actionType',e.target.value)} className={inputClass}><option value="none">Automática pelo texto do botão</option><option value="product-info">Informações do produto</option><option value="scroll">Ir para uma seção</option><option value="url">Abrir link / página</option><option value="whatsapp">Abrir WhatsApp</option><option value="phone">Ligar</option><option value="email">Enviar e-mail</option><option value="top">Voltar ao topo</option></select></label>{type==='none'&&<div className="rounded border border-emerald-500/20 bg-emerald-500/5 px-2 py-1.5 text-[9px] leading-relaxed text-emerald-300">Ação automática ativa: <strong>{automatic.type}</strong>{automatic.value?` → ${automatic.value}`:''}. Assim, botões dos modelos antigos também funcionam sem configuração manual.</div>}{type==='product-info'&&<label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Produto vinculado</span><select value={String(node.props?.actionValue||node.props?.productId||'')} onChange={e=>{prop('actionValue',e.target.value);prop('productId',e.target.value)}} className={inputClass}><option value="">Selecione um produto</option>{catalog.products.map(product=><option key={product.id} value={product.id}>{product.code} · {product.shortDescription}</option>)}</select></label>}{type==='scroll'&&<label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Seção de destino</span><select value={String(node.props?.actionValue||'#catalogo')} onChange={e=>prop('actionValue',e.target.value)} className={inputClass}>{SECTION_OPTIONS.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label>}{type!=='none'&&type!=='top'&&type!=='product-info'&&type!=='scroll'&&<label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Destino</span><input value={String(node.props?.actionValue||'')} onChange={e=>prop('actionValue',e.target.value)} placeholder={type==='url'?'/pagina, #secao ou https://...':type==='whatsapp'?'5527999999999':type==='email'?'contato@empresa.com.br':'+55 27 99999-9999'} className={inputClass}/></label>}{(type==='whatsapp'||type==='email')&&<label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Mensagem / assunto</span><input value={String(node.props?.actionMessage||'')} onChange={e=>prop('actionMessage',e.target.value)} className={inputClass}/></label>}{type==='url'&&<label className="block"><span className="mb-1 block text-[9px] text-zinc-500">Abrir em</span><select value={String(node.props?.actionTarget||'same')} onChange={e=>prop('actionTarget',e.target.value)} className={inputClass}><option value="same">Mesma aba</option><option value="new">Nova aba</option></select></label>}<p className="text-[9px] leading-relaxed text-zinc-500">Todos os botões dos modelos recebem uma ação. Você pode manter a função automática ou substituir por produto, seção, link, WhatsApp, telefone ou e-mail.</p></div>}
