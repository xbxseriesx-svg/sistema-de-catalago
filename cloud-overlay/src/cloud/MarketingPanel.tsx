import{useEffect,useState,type ChangeEvent}from'react';
import{ArrowDown,ArrowUp,BadgePercent,Eye,ImagePlus,MonitorPlay,Palette,Plus,Save,Trash2,Upload,Video,X}from'lucide-react';
import{DEFAULT_MARKETING,productionApi,type MarketingConfig}from'./productionApi';
import{OffersPanel}from'./OffersPanel';

const C='w-full rounded border border-zinc-700 bg-zinc-950 px-2 py-2 text-[10px] text-zinc-100 outline-none focus:border-pink-500';
const B='flex items-center justify-center gap-1 rounded border border-zinc-700 bg-zinc-950 px-2 py-2 text-[9px] font-semibold text-zinc-300';
type Tab='banner'|'video'|'carousel'|'promotions'|'theme';
export function MarketingPanel(){
  const[config,setConfig]=useState<MarketingConfig>(DEFAULT_MARKETING),[saved,setSaved]=useState<MarketingConfig>(DEFAULT_MARKETING),[tab,setTab]=useState<Tab>('banner'),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[preview,setPreview]=useState(false);
  useEffect(()=>{productionApi.getMarketing().then(value=>{setConfig(value);setSaved(value)}).catch(error=>setMessage(error instanceof Error?error.message:'Falha ao carregar Marketing.'))},[]);
  const patch=<K extends keyof MarketingConfig>(key:K,value:Partial<MarketingConfig[K]>)=>setConfig(prev=>({...prev,[key]:{...prev[key],...value}}));
  const save=async()=>{setBusy(true);setMessage('Salvando...');try{const value=await productionApi.saveMarketing(config);setConfig(value);setSaved(value);setMessage('Configurações salvas com sucesso.')}catch(error){setMessage(error instanceof Error?error.message:'Falha ao salvar Marketing.')}finally{setBusy(false)}};
  const cancel=()=>{setConfig(saved);setMessage('Alterações não salvas foram descartadas.')};
  const upload=async(event:ChangeEvent<HTMLInputElement>,kind:'banner'|'video'|'carousel')=>{const file=event.target.files?.[0];event.target.value='';if(!file)return;setBusy(true);setMessage(`Enviando ${file.name}...`);try{if(kind==='banner'){const media=await productionApi.uploadMedia(file,file.type.startsWith('video/')?'marketing-video':'marketing-banner','main');patch('banner',{mediaUrl:media.url,mediaType:file.type.startsWith('video/')?'video':'image'})}else if(kind==='video'){const media=await productionApi.uploadMedia(file,'marketing-video','video-banner');patch('videoBanner',{mediaUrl:media.url})}else{const id=crypto.randomUUID();const media=await productionApi.uploadMedia(file,'carousel-image',id);patch('carousel',{items:[...config.carousel.items,{id,url:media.url,link:'',alt:file.name}]})}setMessage('Mídia salva permanentemente. Clique em Salvar para aplicar a configuração.')}catch(error){setMessage(error instanceof Error?error.message:'Falha no upload.')}finally{setBusy(false)}};
  const move=(index:number,direction:-1|1)=>setConfig(prev=>{const items=[...prev.carousel.items],target=index+direction;if(target<0||target>=items.length)return prev;[items[index],items[target]]=[items[target],items[index]];return{...prev,carousel:{...prev.carousel,items}}});
  const remove=(index:number)=>setConfig(prev=>({...prev,carousel:{...prev.carousel,items:prev.carousel.items.filter((_,i)=>i!==index)}}));
  return <div className="rounded-xl border border-zinc-800 bg-zinc-900"><div className="border-b border-zinc-800 p-3"><div className="flex items-center gap-2"><MonitorPlay size={14} className="text-pink-400"/><div><h3 className="text-xs font-bold text-zinc-100">Marketing</h3><p className="text-[9px] text-zinc-500">Banner, vídeo, carrossel, promoções e identidade visual.</p></div></div>{message&&<div className="mt-2 rounded border border-zinc-800 bg-zinc-950 p-2 text-[9px] text-zinc-300">{message}</div>}<div className="mt-3 grid grid-cols-5 gap-1">{([['banner','Banner',ImagePlus],['video','Vídeo',Video],['carousel','Carrossel',MonitorPlay],['promotions','Promoções',BadgePercent],['theme','Tema',Palette]] as const).map(([id,label,Icon])=><button key={id} onClick={()=>setTab(id)} className={`flex min-h-12 flex-col items-center justify-center gap-1 rounded text-[8px] font-bold ${tab===id?'bg-pink-600 text-white':'bg-zinc-800 text-zinc-500 hover:text-zinc-200'}`}><Icon size={11}/>{label}</button>)}</div></div>{tab==='promotions'?<div className="p-2"><OffersPanel/></div>:<div className="p-3">{tab==='banner'&&<Banner config={config} patch={patch} upload={upload} busy={busy}/>} {tab==='video'&&<VideoBanner config={config} patch={patch} upload={upload} busy={busy}/>} {tab==='carousel'&&<Carousel config={config} patch={patch} upload={upload} busy={busy} move={move} remove={remove}/>} {tab==='theme'&&<Theme config={config} patch={patch}/>}<div className="mt-3 grid grid-cols-3 gap-2"><button disabled={busy} onClick={()=>void save()} className="flex items-center justify-center gap-1 rounded bg-pink-600 py-2 text-[10px] font-bold text-white disabled:opacity-50"><Save size={11}/>Salvar</button><button disabled={busy} onClick={cancel} className={B}><X size={11}/>Cancelar</button><button onClick={()=>setPreview(v=>!v)} className={B}><Eye size={11}/>Prévia</button></div>{preview&&<Preview config={config}/>}</div>}</div>}
function Toggle({label,value,onChange}:{label:string;value:boolean;onChange:(value:boolean)=>void}){return <label className="flex items-center justify-between rounded border border-zinc-800 bg-zinc-950/50 px-2 py-2 text-[9px] text-zinc-400"><span>{label}</span><input type="checkbox" checked={value} onChange={e=>onChange(e.target.checked)} className="accent-pink-500"/></label>}
function Banner({config,patch,upload,busy}:{config:MarketingConfig;patch:any;upload:any;busy:boolean}){const b=config.banner;return <div><Title icon={<ImagePlus size={12}/>} text="Banner principal"/><div className="mt-2 grid grid-cols-2 gap-2"><Toggle label="Ativo" value={b.active} onChange={v=>patch('banner',{active:v})}/><select className={C} value={b.mediaType} onChange={e=>patch('banner',{mediaType:e.target.value})}><option value="image">Imagem</option><option value="video">Vídeo</option></select></div><label className={`${B} mt-2 cursor-pointer ${busy?'pointer-events-none opacity-50':''}`}><Upload size={11}/>Importar imagem ou vídeo<input hidden type="file" accept="image/*,video/*" onChange={e=>upload(e,'banner')}/></label>{b.mediaUrl&&<MediaPreview url={b.mediaUrl} video={b.mediaType==='video'}/>}<input className={`${C} mt-2`} value={b.title} onChange={e=>patch('banner',{title:e.target.value})} placeholder="Título"/><input className={`${C} mt-2`} value={b.subtitle} onChange={e=>patch('banner',{subtitle:e.target.value})} placeholder="Subtítulo"/><input className={`${C} mt-2`} value={b.link} onChange={e=>patch('banner',{link:e.target.value})} placeholder="Link"/>{b.mediaType==='video'&&<div className="mt-2 grid grid-cols-3 gap-1"><Toggle label="Auto" value={b.autoplay} onChange={v=>patch('banner',{autoplay:v})}/><Toggle label="Loop" value={b.loop} onChange={v=>patch('banner',{loop:v})}/><Toggle label="Mudo" value={b.muted} onChange={v=>patch('banner',{muted:v})}/></div>}</div>}
function VideoBanner({config,patch,upload,busy}:{config:MarketingConfig;patch:any;upload:any;busy:boolean}){const v=config.videoBanner;return <div><Title icon={<Video size={12}/>} text="Banner de vídeo"/><Toggle label="Ativar vídeo" value={v.active} onChange={value=>patch('videoBanner',{active:value})}/><label className={`${B} mt-2 cursor-pointer ${busy?'pointer-events-none opacity-50':''}`}><Upload size={11}/>Upload de vídeo<input hidden type="file" accept="video/*" onChange={e=>upload(e,'video')}/></label>{v.mediaUrl&&<MediaPreview url={v.mediaUrl} video/>}<input className={`${C} mt-2`} value={v.title} onChange={e=>patch('videoBanner',{title:e.target.value})} placeholder="Título"/><input className={`${C} mt-2`} value={v.subtitle} onChange={e=>patch('videoBanner',{subtitle:e.target.value})} placeholder="Subtítulo"/><div className="mt-2 grid grid-cols-2 gap-1"><Toggle label="Reprodução automática" value={v.autoplay} onChange={value=>patch('videoBanner',{autoplay:value})}/><Toggle label="Loop infinito" value={v.loop} onChange={value=>patch('videoBanner',{loop:value})}/><Toggle label="Áudio mudo" value={v.muted} onChange={value=>patch('videoBanner',{muted:value})}/><Toggle label="Controles" value={v.controls} onChange={value=>patch('videoBanner',{controls:value})}/></div></div>}
function Carousel({config,patch,upload,busy,move,remove}:{config:MarketingConfig;patch:any;upload:any;busy:boolean;move:(index:number,direction:-1|1)=>void;remove:(index:number)=>void}){const c=config.carousel;return <div><Title icon={<MonitorPlay size={12}/>} text="Carrossel"/><div className="mt-2 grid grid-cols-2 gap-2"><Toggle label="Ativo" value={c.active} onChange={v=>patch('carousel',{active:v})}/><select className={C} value={String(c.speed)} onChange={e=>patch('carousel',{speed:Number(e.target.value)})}><option value="1">1x</option><option value="1.5">1,5x</option><option value="2">2x</option></select></div><div className="mt-2 grid grid-cols-3 gap-1"><Toggle label="Loop" value={c.loop} onChange={v=>patch('carousel',{loop:v})}/><Toggle label="Auto" value={c.autoplay} onChange={v=>patch('carousel',{autoplay:v})}/><Toggle label="Manual" value={c.manual} onChange={v=>patch('carousel',{manual:v})}/></div><label className={`${B} mt-2 cursor-pointer ${busy?'pointer-events-none opacity-50':''}`}><Plus size={11}/>Adicionar imagem<input hidden type="file" accept="image/*" onChange={e=>upload(e,'carousel')}/></label><div className="mt-2 space-y-1">{c.items.map((item,index)=><div key={item.id} className="flex items-center gap-2 rounded border border-zinc-800 bg-zinc-950/50 p-2"><img src={item.url} alt={item.alt} className="h-12 w-16 rounded bg-white object-contain"/><div className="min-w-0 flex-1"><input className={C} value={item.alt} onChange={e=>patch('carousel',{items:c.items.map((x,i)=>i===index?{...x,alt:e.target.value}:x)})} placeholder="Descrição"/><input className={`${C} mt-1`} value={item.link} onChange={e=>patch('carousel',{items:c.items.map((x,i)=>i===index?{...x,link:e.target.value}:x)})} placeholder="Link"/></div><div className="grid gap-1"><button disabled={index===0} onClick={()=>move(index,-1)} className="text-zinc-500 disabled:opacity-20"><ArrowUp size={11}/></button><button disabled={index===c.items.length-1} onClick={()=>move(index,1)} className="text-zinc-500 disabled:opacity-20"><ArrowDown size={11}/></button><button onClick={()=>remove(index)} className="text-red-400"><Trash2 size={11}/></button></div></div>)}</div></div>}
function Theme({config,patch}:{config:MarketingConfig;patch:any}){const t=config.theme;const colors=[['primary','Cor principal'],['secondary','Cor secundária'],['background','Fundo'],['surface','Superfície'],['text','Texto']] as const;return <div><Title icon={<Palette size={12}/>} text="Tema e cores"/><select className={`${C} mt-2`} value={t.mode} onChange={e=>{const mode=e.target.value as MarketingConfig['theme']['mode'];if(mode==='light')patch('theme',{mode,background:'#ffffff',surface:'#f4f4f5',text:'#18181b'});else if(mode==='dark')patch('theme',{mode,background:'#09090b',surface:'#18181b',text:'#fafafa'});else patch('theme',{mode})}}><option value="light">Claro</option><option value="dark">Escuro</option><option value="custom">Personalizado</option></select><div className="mt-2 grid grid-cols-2 gap-2">{colors.map(([key,label])=><label key={key} className="rounded border border-zinc-800 bg-zinc-950/50 p-2"><span className="mb-1 block text-[9px] text-zinc-500">{label}</span><div className="flex items-center gap-2"><input type="color" value={t[key]} onChange={e=>patch('theme',{mode:'custom',[key]:e.target.value})} className="h-9 w-11 cursor-pointer rounded border-0 bg-transparent"/><input className={C} value={t[key]} onChange={e=>patch('theme',{mode:'custom',[key]:e.target.value})}/></div></label>)}</div><div className="mt-3 rounded-lg p-4" style={{background:t.background,color:t.text,border:`1px solid ${t.primary}`}}><div className="font-bold" style={{color:t.primary}}>Visualização em tempo real</div><p className="mt-1 text-xs">A cor aplicada aqui será usada no site público após Salvar.</p><button className="mt-3 rounded px-3 py-2 text-xs font-bold text-white" style={{background:t.primary}}>Botão principal</button></div></div>}
function Preview({config}:{config:MarketingConfig}){return <div className="mt-3 overflow-hidden rounded-lg border border-zinc-700" style={{background:config.theme.background,color:config.theme.text}}><div className="px-2 py-1 text-[8px] font-black uppercase opacity-50">Pré-visualização</div>{config.banner.active&&config.banner.mediaUrl&&(config.banner.mediaType==='video'?<video src={config.banner.mediaUrl} autoPlay={config.banner.autoplay} loop={config.banner.loop} muted={config.banner.muted} controls className="max-h-52 w-full object-cover"/>:<img src={config.banner.mediaUrl} alt="Banner" className="max-h-52 w-full object-cover"/>)}{config.banner.active&&(config.banner.title||config.banner.subtitle)&&<div className="p-3"><div className="font-bold" style={{color:config.theme.primary}}>{config.banner.title}</div><div className="text-xs opacity-60">{config.banner.subtitle}</div></div>}{config.videoBanner.active&&config.videoBanner.mediaUrl&&<video src={config.videoBanner.mediaUrl} autoPlay={config.videoBanner.autoplay} loop={config.videoBanner.loop} muted={config.videoBanner.muted} controls={config.videoBanner.controls} className="max-h-52 w-full object-cover"/>}</div>}
function MediaPreview({url,video=false}:{url:string;video?:boolean}){return <div className="mt-2 overflow-hidden rounded border border-zinc-800 bg-black">{video?<video src={url} controls className="max-h-40 w-full object-contain"/>:<img src={url} alt="Prévia" className="max-h-40 w-full bg-white object-contain"/>}</div>}
function Title({icon,text}:{icon:React.ReactNode;text:string}){return <div className="mb-2 flex items-center gap-2 text-[10px] font-bold uppercase text-zinc-300">{icon}{text}</div>}
